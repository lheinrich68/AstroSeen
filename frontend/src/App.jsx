import Router from './routes/Router.jsx';

/**
 * Starting point of the application. It defines the routes and renders the main layout.
 * @returns {JSX.Element} The rendered application.
 */

export default function App() {
  return (
      <>
        <Router />
      </>
  )
}